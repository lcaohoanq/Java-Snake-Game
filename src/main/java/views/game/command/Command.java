package views.game.command;

/**
 * Command Pattern interface
 * Defines the contract for all game commands
 */
public interface Command {
    
    /**
     * Execute the command
     */
    void execute();
}